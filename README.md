# SecureScript_PowerShell
SecureScript_PowerShell AES encrypt

Vnc_log script display last users info for remotely host  in readable view.
For it go to log file VNC application. File is located in windows host. Passworrd is encrypted via AES.


